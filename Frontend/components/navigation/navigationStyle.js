import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  navigationHeader:{
    textAlign: 'center',
    paddingTop: 30,
    paddingBottom: 30,
    fontSize: 20
  }

});
