import { StyleSheet } from 'react-native';

export default  styles = StyleSheet.create({
   HeaderText: {
     color: '#fff'
   },
   NormalText: {
     fontSize: 14,
     fontWeight: 'bold'
   },
   NestedText: {
     color: "#333",
     fontWeight: 'normal'
   },
   Hyperlink: {
     color: "#44f",
     fontWeight: 'normal'
   }
});
